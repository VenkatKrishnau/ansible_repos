- name: Learning variables with prompt
  hosts: localhost
  vars_prompt:
  - name: VALUE1
    prompt: enter value1
    private: false
  tasks:
    - name: reading value from prompt
      ansible.builtin.debug:
        msg: value of is {{VALUE1}}