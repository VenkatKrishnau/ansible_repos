- name: Install packages 
  hosts: web
  become: yes 

  tasks:
    - name: install packages in node
      ansible.builtin.dnf:
        msg:  "{{ item }}"
        state: latest
      loop:
      - mysql
      - nginx
      - postfix
